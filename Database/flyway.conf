# =============================================================================
# Flyway Configuration for TEMCO AdminApp
# =============================================================================
# Usage:
#   Local:  flyway -configFiles=Database/flyway.conf migrate
#   CI/CD:  Override url/user/password via environment variables
# =============================================================================

# =============================================================================
# DEPRECATED — DO NOT USE
# =============================================================================
# All Flyway migrations for temco_system are now owned by SSOService.
# See: F:\TemcoERP\SSOService\Database\flyway.conf
# Migration files in this directory are kept for historical reference only.
# =============================================================================
#
# flyway.url=jdbc:mysql://${DB_HOST:-localhost}:${DB_PORT:-3306}/${DB_NAME:-temco_system}
# flyway.user=${DB_USER:-root}
# flyway.password=${DB_PASSWORD:-6qZB6d@pIvj}
# flyway.locations=filesystem:Database/migrations
# flyway.baselineOnMigrate=true
# flyway.baselineVersion=0
# flyway.outOfOrder=false
# flyway.validateOnMigrate=true
